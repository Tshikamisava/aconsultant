# Environment variables for A Consultant website
# Copy this file to .env and fill in your values

# Gmail SMTP Configuration for Netlify Functions
EMAIL_USER=your-email@example.com
EMAIL_PASS=your-app-password

# API URL (if using a backend service)
# VITE_API_URL=https://your-api-domain.com

# Note: For production deployment on Netlify:
# 1. Go to Site Settings > Environment Variables  
# 2. Add EMAIL_USER with your actual Gmail address
# 3. Add EMAIL_PASS with your Gmail app password
# 4. Do NOT commit these values to the repository
